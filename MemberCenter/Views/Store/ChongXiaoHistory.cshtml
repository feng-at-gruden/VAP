@model IEnumerable<MemberCenter.Models.ConsumptionViewModel>
@{
    ViewBag.Title = "商城重消消费记录";
}

<div class="panel panel-default">
    <div class="panel-heading">
        STORE CONSUMPTION RECORDS
    </div>
    <div class="panel-body">
        <div class="dataTable_wrapper">
            <table class="table table-striped table-bordered" id="dataTables-example" style="text-align:center;">
                <thead>
                    <tr class="primary">
                        <th style="text-align:right;">@Html.DisplayNameFor(m => m.Amount)</th>
                        <th class="center-cell">@Html.DisplayNameFor(m => m.Type)</th>
                        <th class="center-cell">@Html.DisplayNameFor(m => m.Comment)</th>
                        <th class="center-cell">@Html.DisplayNameFor(m => m.DateTime)</th>
                    </tr>
                </thead>
                <tbody>
                    @foreach (var item in Model)
                    {
                        <tr class="odd">
                            <td style="text-align:right;">@Html.DisplayFor(modelItem => item.Amount)</td>
                            <td>@Html.DisplayFor(modelItem => item.Type)</td>
                            <td>@Html.DisplayFor(modelItem => item.Comment)</td>
                            <td>@Html.DisplayFor(modelItem => item.DateTime)</td>
                        </tr>
                    }
                </tbody>
            </table>
        </div>
        <!-- /.Table -->
    </div>
    <!-- /.panel-body -->
    <div class="panel-footer">
        &nbsp;
    </div>
</div>

<script>
    $(document).ready(function () {
        $('#dataTables-example').DataTable({
            "responsive": true,
            "paging": true,
            "ordering": false,
            "info": false,
            "searching": false,
        });
    });
</script>