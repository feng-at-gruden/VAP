@using Backend.Models
@model PagedList.IPagedList< Backend.Models.SystemSetting >

@{
    ViewBag.Title = "系统设置";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<div class="place">
    <span>位置：</span>
    <ul class="placeul">
        <li><a href="@Url.Action("Index","Home")">首页</a></li>
        <li><a href="#">系统设置</a></li>
    </ul>
</div>

@using (Html.BeginForm("MetaIndex", "Admin", FormMethod.Get))
{
    <div>
       @* @{
    var options = (from scl in VapLib.Constants.MetaType
                   select new SelectListItem
                   {
                       Text = scl,
                       Value = scl
                   }).ToList();
    options.Insert(0, new SelectListItem
    {
        Text = "请选择类型",
        Value = ""
    });
        }

        @Html.DropDownList("metaType", options, new { @class = "form-control" })<br />
*@

        @Html.TextBox("SearchString", ViewBag.CurrentFilter as string, new { @class = "form-control", placeholder = "输入名称" })<br />
        <input type="submit" class="btn btn-primary" value="查询" />
    </div><br />
}
<table class="table">
    <tr>
        <th>
            类型
        </th>
        <th>
           名称
        </th>
        <th>
            简码
        </th>
        <th>操作</th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Id)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Key)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Value)
        </td>
        <td> 
            @Html.ActionLink("修改", "EditMeta", new { id = item.Id }, new { @class = "tablelink" }) |
            @Html.ActionLink("删除", "DeleteMeta", new { id = item.Id }, new {@class="tablelink", onclick = "javascript:return confirm('确认删除？')" })
        </td>
    </tr>
}

</table>
<br />
@Html.Partial("_Pager", Model)